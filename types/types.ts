export type User = {
  id: string;
  username: string;
  email: string;
  role: string;
};

export type PageMeta = {
  title: string;
  metedesc: string;
};

export type UserMeta = {
  email: string;
  role: string;
};
